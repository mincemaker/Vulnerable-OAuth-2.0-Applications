# Copy to .env (or export in your shell) to override defaults at startup.
# Defaults match insecureapplication/docker-compose.yml's defaults if unset.
CLIENT_ID=photoprint
CLIENT_SECRET=secret
SESSION_SECRET=changethistoconfigfile
# GALLERY_URL=http://gallery:3005
# GALLERY_BROWSER_URL=http://gallery.127.0.0.1.nip.io:3005
