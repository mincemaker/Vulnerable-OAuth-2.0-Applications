# Copy to .env (or export in your shell) to override the seeded
# "photoprint" OAuth client's clientID/clientSecret at startup, mirroring
# insecureapplication/mongo-seed/seed.sh. Defaults match
# insecureapplication/docker-compose.yml's defaults if unset.
CLIENT_ID=photoprint
CLIENT_SECRET=secret
